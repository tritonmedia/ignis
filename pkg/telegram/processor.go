package telegram

import (
	"github.com/tritonmedia/ignis/pkg/state"
	"gopkg.in/telegram-bot-api.v4"
)

// processMessage runs an action when a message is recieved
func processMessage(msg *tgbotapi.Message, s *state.State, u *state.User) error {
	return nil
}
